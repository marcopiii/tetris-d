export type PlaneCoords = { x: number } | { z: number };

export type LineCoord = { y: number; x: number } | { y: number; z: number };

export type MinoCoord = {
  y: number;
  x: number;
  z: number;
};
