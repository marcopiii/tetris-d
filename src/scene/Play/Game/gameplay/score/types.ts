export type Progress = {
  score: number;
  level: number;
};

export type PlaneCombo = 'mono' | 'parallel' | 'orthogonal';

export type TSpinKind = 'mini' | 'full';
export type ZicKind = 'mini' | 'full';
