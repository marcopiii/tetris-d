import {renderBlock} from "./renderBlock.js";
import type {Piece} from "../models/3DPiece";

export function renderPiece(ctx, piece: Piece) {
  // const {shape, color, position} = piece;
  // for (let z = 0; z < shape.length; z++) {
  //   for (let y = 0; y < shape[z].length; y++) {
  //     for (let x = 0; x < shape[z][y].length; x++) {
  //       if (shape[z][y][x] !== 0) {
  //         renderBlock(ctx, position.x + x, position.y + y, position.z + z, color);
  //       }
  //     }
  //   }
  // }
}
